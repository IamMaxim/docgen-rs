# Introduction

Some intro prose linking back to [[index]] and a [[missing-page]] one.

## Getting started

A second-level heading so the "On this page" rail TOC and scroll-spy have
something to anchor.

### Prerequisites

A third-level heading to exercise the `is-depth-3` indentation path.

```rust
fn main() { println!("hi"); }
```

## Next steps

Another top-level section to round out the outline.
