---
title: docgen 0.7.0
date: 2026-07-16
version: 0.7.0
kind: minor
areas: [s3]
crates: 13
sidebar: false        # surfaced on the Releases page, not the sidebar
tags: [release]
---

A single fix, but a load-bearing one: **[[s3-offload|S3 offload]] over TLS**.

The `attohttpc` client pulls in rustls, and rustls insists on exactly one active
crypto provider. Two were being linked in, so provider selection was ambiguous
and HTTPS requests to the bucket could not complete. This release pins rustls to
one provider, restoring `https://` endpoints.

The story does not quite end here — a follow-up in [[v0-8-1|0.8.1]] proves the
TLS path end-to-end against a live MinIO server and hardens the provider guard
that this release put in place.
